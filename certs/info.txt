This folder is used to store SSL certificates.
Save the certificate files as server.crt.pem and server.key.pem, and the install_ingress-nginx.sh script will automatically read them from this folder when setting up Ingress.